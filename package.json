{
  "name": "fittrack",
  "version": "0.1.0",
  "description": "Vanilla JS fitness tracker with localStorage persistence",
  "scripts": {
    "format": "prettier --check \"**/*.{html,css,js,json,md}\"",
    "format:fix": "prettier --write \"**/*.{html,css,js,json,md}\"",
    "test": "node --test tests/",
    "lint": "eslint .",
    "build": "echo 'No build step needed for vanilla JS'"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5"
  }
}
